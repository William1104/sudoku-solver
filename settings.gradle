rootProject.name = 'sudoku-solver'

include 'java'
include 'kotlin'
include 'kotlin-2'

buildCache {
    local {
        // Set local build cache directory.
        directory = "${settingsDir}/build-cache"
    }
}